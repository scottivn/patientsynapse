# PatientSynapse — Python dependencies

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
pydantic-settings>=2.0

# HTTP client
httpx>=0.27.0

# SMART on FHIR auth
PyJWT>=2.9.0
cryptography>=43.0

# App auth
passlib[bcrypt]>=1.7.4
aiosqlite>=0.20.0

# LLM providers (grok uses httpx directly)
boto3>=1.35.0               # AWS Bedrock (HIPAA-eligible)
# openai>=1.50.0          # Uncomment when using OpenAI
# anthropic>=0.39.0       # Uncomment when using Anthropic

# Excel import (allowable rates)
openpyxl>=3.1.0

# OCR / PDF
pypdf>=4.0
PyMuPDF>=1.24
Pillow>=10.0
pytesseract>=0.3.10

# MCP server
mcp[cli]>=1.0

# Dev
python-dotenv>=1.0
python-multipart>=0.0.9

# Testing
pytest>=8.0
pytest-asyncio>=0.24.0
